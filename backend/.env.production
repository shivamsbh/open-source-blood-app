# Production Environment Variables for Backend
# These should be set in Render dashboard, not committed to git

# MongoDB Atlas Connection (set in Render)
# MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/bloodbank?retryWrites=true&w=majority

# JWT Secret (set in Render)
# JWT_SECRET=your_super_secure_jwt_secret_key_for_production

# Server Configuration
NODE_ENV=production
DEV_MODE=production
PORT=10000

# CORS Origins (update with your actual Vercel URL)
FRONTEND_URL=https://your-app-name.vercel.app